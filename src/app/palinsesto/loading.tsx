import PalinsestoSkeleton from './PalinsestoSkeleton';

/**
 * Skeleton a livello di rotta: mostrato da Next.js automaticamente
 * mentre il Server Component sta per essere eseguito.
 */
export default function Loading() {
  return <PalinsestoSkeleton />;
}
